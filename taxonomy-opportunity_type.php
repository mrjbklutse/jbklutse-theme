<?php
/**
 * Taxonomy archive for /opportunities/<type>/ — reuses archive-opportunity.php
 * since the layout is identical. Type tabs in the parent template highlight
 * the current term automatically via is_tax() check.
 */
include get_theme_file_path( 'archive-opportunity.php' );
